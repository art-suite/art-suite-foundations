"use strict"
let Caf = require('caffeine-script-runtime');
Caf.defMod(module, () => {return Caf.importInvoke(["assert", "lowerCamelCase", "isPromise", "isFunction", "merge", "Function"], [global, require('art-standard-lib'), require('../Presentation'), require('chai'), require('./ArtChaiLib')], (assert, lowerCamelCase, isPromise, isFunction, merge, Function) => {return Caf.each2(["missing", "clientFailure", "clientFailureNotAuthorized"], (status) => {let name; return assert[status] = assert[name = lowerCamelCase(`is ${Caf.toString(status)}`)] = function(communicationPromise, context) {let communicationResult, temp; return (isPromise(communicationPromise) || isFunction(communicationPromise)) ? assert.rejects(communicationPromise).then((response) => {assert.eq(response.status, status, (...contextArgs) => merge({response, context: (Caf.is(context, Function)) ? context(...contextArgs) : context})); return response;}) : (communicationResult = communicationPromise, assert.eq(((temp = Caf.exists(communicationResult) && communicationResult.status) != null ? temp : communicationResult), status, context));};});});});
//# sourceMappingURL=CommunicationStatusAsserts.js.map
