"use strict"
let Caf = require('caffeine-script-runtime');
Caf.defMod(module, () => {return Caf.importInvoke(["log", "formattedInspect", "merge", "String"], [global, require('art-standard-lib')], (log, formattedInspect, merge, String) => {return {logResult: function(email, result, context) {let core, valid, invalid, reason, smtpStatus, smtpStatusSubject, smtpStatusCode, smtpEnhancedStatusCode; core = ({valid, invalid, reason, smtpStatus, smtpStatusSubject, smtpStatusCode, smtpEnhancedStatusCode} = result, {valid, invalid, reason, smtpStatus, smtpStatusSubject, smtpStatusCode, smtpEnhancedStatusCode}); log(`Art.EmailValidator ${Caf.toString(context)} ${Caf.toString(email)}: ` + formattedInspect(merge(core), 100000)); return (!valid) ? Caf.each2(result, (value, key) => log(`Art.EmailValidator ${Caf.toString(context)} ${Caf.toString(email)}:   ${Caf.toString(key)}: ` + formattedInspect(value.replace(/[\s\r\n]+/g, " "))), (value, key) => !(core[key] != null) && Caf.is(value, String)) : undefined;}};});});
//# sourceMappingURL=Verbose.js.map
