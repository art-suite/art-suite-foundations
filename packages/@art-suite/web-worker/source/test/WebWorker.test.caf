import &StandardImport

describe
  inNode: ->
    test "isWebWorker should be false" -> assert.eq isWebWorker, false
    test "webWorkersSupported should be false" -> assert.eq webWorkersSupported, false
