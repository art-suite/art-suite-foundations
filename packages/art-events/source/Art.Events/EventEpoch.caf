import &ArtStandardLib

class EventEpoch extends &ArtEpochedState.EpochClass
  @singletonClass()

  # event is a function
  # "null" is allowed and ignored
  # returns the event passed in
  queue:    (event) -> @queueItem event
  logEvent: (name, id) ->
